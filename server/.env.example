# Server Configuration
PORT=8000
NODE_ENV=production

# Database Configuration
MONGODB_URI=mongodb+srv://ynsocial:Graf2021@cluster0.wb86x.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0

# Authentication
JWT_SECRET=adlords-jwt-secret-2024
JWT_EXPIRY=24h

# CORS
CORS_ORIGIN=https://adlords.netlify.app

# Redis Configuration
REDIS_URL=redis://default:${REDIS_PASSWORD}@${REDIS_HOST}:${REDIS_PORT}

# Email Configuration (if needed)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=

# AWS Configuration (if needed)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=

# Logging
LOG_LEVEL=info
